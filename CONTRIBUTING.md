# Contributing

Use synthetic fixtures only. Run `python -m unittest discover -s tests -v`. Describe the concrete failure a change addresses and its limits. Changes that add live connectors, model inference or external actions require an explicit threat model, permission design and independent evaluation plan. Preserve human decision authority and source provenance. Do not add real customer documents or secrets.
